import { Placeholder } from "@/components/shell/placeholder";

export const metadata = { title: "Combine Payment" };

export default function Page() {
  return <Placeholder href="/combine-payment" />;
}
